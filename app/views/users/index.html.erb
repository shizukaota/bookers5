<div class="container">
  <div class="row">
      <%= render'books/list',book:@bookn,user: current_user %>

    <div class="col-xs-9">
      <h2>Users </h2>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>image</th>
            <th>name</th>
            <tH>show</th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
          <tr>

            <td><%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg',fallback: "no_image.jpg" %></td>
              <td><%=user.name%></td>

            <td>""
              <%= link_to user_path(user),class: "users" do  %>
                show
	             <% end %>
	          </td>
          </tr>
        <% end %>

        </tbody>
      </table>
    </div>